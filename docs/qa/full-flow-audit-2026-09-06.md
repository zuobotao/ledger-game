# 全流程自测计划与迭代记录

日期：2026-09-06

目标：围绕手机端和 PC 端，覆盖从入口、配置、回合操作、银行/贷款、资本游戏、梦想、历史到结束页的可点击链路，并记录每个修复迭代的证据。

## 覆盖链路

| 区域 | 关键分支 | 验收重点 |
| --- | --- | --- |
| 首页 | 新游戏、继续游戏、规则、新手简介、多人房间、历史 | 入口可达、文案完整、返回路径正确 |
| 设置 | 1/2/多人、职业详情/随机、梦想详情/随机、颜色、AI、可选规则 | 表单状态保存、信息按钮、手机不溢出 |
| 老鼠圈 | 掷骰、机会/市场/慈善/孩子/失业/梦想、买卖/贷款/保险、玩家切换 | 操作反馈、拒绝原因、待处理动作不丢失 |
| 银行 | 存款/取款、贷款/还款、保险、财务报表 | 成功与拒绝均有明确原因，金额和状态同步 |
| 资本游戏 | 资格条、进入条件、棋盘、梦想目标、退出/胜利 | 资格信息在移动端可读，棋盘完整可操作 |
| 房间 | 创建、加入、设置、准备、开始、断线/状态广播 | X/Y 就绪人数和玩家状态一致，PC/手机可混玩 |
| 结束 | 胜利、破产、退休、历史记录 | 结果信息、重新开始、返回首页链路完整 |

## 设备矩阵

- PC：1280×800，逐页点击和关键分支截图。
- 手机：390×844，逐页点击、横向溢出检查、固定区/弹窗可达性检查。
- 多人：两个浏览器上下文，覆盖创建、加入、准备、开始和同步。

## 迭代记录

| 提交 | 范围 | 验证 |
| --- | --- | --- |
| `46bb60d` | 贷款拒绝原因 | 贷款单元测试通过；银行面板沿用具体失败文案 |
| `7f91f21` | 房间就绪人数和玩家状态 | 多人双浏览器房间 E2E 通过，显示 `已准备 X / Y` |
| `c0c28cc` | 可选规则说明 | 设置页 PC/手机回归通过，四项规则均有用途说明 |
| `cfd8d6d` + `7a1e345` | 梦想信息按钮与详情 | 设置页、多人大厅梦想详情回归通过 |
| `4ba6302` | 手机棋盘适配 | 390×844 竖屏、844×390 横屏无横向溢出，棋盘在容器内完整可见 |
| `c73e9f1` | 移动端资格栏 | 紧凑顶部条，详情默认收起；桌面保留展开详情 |
| `9bf4eb5` + `b951a47` | 自测动作桥反馈状态 | 反馈按钮按真实 DOM 可见性判断；用于避免历史状态阻断后续动作 |
| `1afb2d2` | 房间刷新恢复与继续游戏反馈 | 大厅自身接管刷新重连；加入/恢复超时、失效会话和重试有明确状态；继续游戏点击时重新读取同源存档并显示恢复中/失败反馈 |
| `09b3599` | 资本游戏购买信息遮挡 | 待处理动作面板限制在棋盘区域内并支持内部滚动；桌面和手机购买信息、数量和现金提示均可见 |
| `2690610` | 财务报表负债操作恢复 | 修复可选 Boolean prop 默认值导致侧栏“还清/还贷”按钮全部隐藏；负债行增加稳定定位；银行弹窗与侧栏重复控件均按容器隔离，完整单人财务闭环通过 |
| `43c643e` | 手机棋盘与主操作区 | 修复移动端棋盘 section 被 flex 压缩后裁掉掷骰/结束回合按钮；竖屏和横屏均为主操作区预留空间，并增加指针命中与遮挡回归断言 |
| `ce1aff1` | 快车道自测假卡死与假通过 | 专项用例复用 DOM 可见反馈状态，按真实顺序选择股票后检查数量，并识别市场事件“结束”按钮；新增正常收尾断言，耗尽动作上限时必须失败 |
| `3d6c79a` | 房间意外断线重连 | 旧 socket 关闭时先从客户端解绑，再触发重连；忽略过期 socket 的事件，重连沿用 10 秒超时边界，避免界面无期限停在恢复状态 |
| 待补 | 全流程剩余分支与部署复验 | 当前基础/随机 Bot 长链路仍在市场事件后复现卡住，见下方未闭环项 |

## 本轮验证记录

- `npm test -- --run test/unit/v24-join-flow.spec.ts test/unit/savings.spec.ts`：2 个文件、19 个测试通过。
- `npm test -- --run test/unit test/integration --reporter=dot`：44 个文件、429 个测试通过。
- `npm run build`：类型检查和 Vite 生产构建通过。
- Playwright：房间刷新恢复和多人完整流程通过；跨窗口继续游戏通过；入口 PC/手机导航 3/3 通过。
- Playwright：资本游戏购买面板桌面/手机定向回归 3/3 通过（含跨窗口恢复）；`fasttrack-basic-game` 手机流程通过，桌面 fixture 流程在触发快车道决策前因回合未推进而失败，未覆盖本次面板断言。
- Playwright：`single-finance-flow` 单人财务闭环通过，覆盖存款、取款、贷款、还款、学生贷款清偿、健康保险、失业保险和财务报表；此前失败根因是 `showActions` Boolean prop 默认 false，以及银行弹窗与侧栏控件未隔离。
- Playwright：手机布局 390×844 与横屏 844×390 均通过；新增断言确认棋盘 section 包含掷骰/结束回合按钮，按钮中心点由自身接收指针事件。修复前横屏断言可复现“按钮被侧栏覆盖”。
- Playwright：修正快车道专项测试对历史反馈状态和市场“结束”按钮的错误识别后，Desktop 1280×800 与 Mobile 390×844 均推进至第 9 回合并正常收尾，2/2 通过；移动端同时覆盖股票买卖和刷新恢复。修复前先复现 `turn 1→1, endTurn=0`，随后又用收尾断言捕获市场事件被重复扫描 291 次的假通过。
- Vitest：RoomClient 意外关闭后立即创建 replacement socket 的红灯已复现，修复后 replacement 发送 reconnect 凭据且保持 open；与房间加入流超时回归共 11 项通过。`npm run build` 通过。
- 随机 Bot 长链路：市场事件卖出后，截图中 `market-dismiss` 按钮可见，但动作桥未继续执行，回合守卫在第 48 回合停止。证据保存在 `playtest/runs/20260906-224642/` 与对应 Playwright trace；该项仍需下一轮定位 `canAct`/动作扫描状态。

## 下一轮排队需求

1. 文字与法律风险审查：统一教育用途、非投资建议、虚构数据等提示，保持短句和可记忆术语。
2. 资本游戏整体审计：样式、文案、流程完整性和卡住分支逐项覆盖 PC/手机/混合房间。
3. 个人财报增强：成本、收益、机会成本、税务和现金流用同一口径展示。
4. 历史复盘与中途保存：先定义本地匿名存档与邀请制账户边界，再决定登录与服务端持久化。
5. 邀请制账户安全设计：邀请码、会话、重放防护和限流需在产品方案确认后实现。

每轮提交前必须记录：修改文件、复现路径、修复行为、桌面结果、手机结果、自动化命令和部署状态。
