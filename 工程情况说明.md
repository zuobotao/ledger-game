# Ledger 财商游戏 - 工程情况说明

## 一、项目背景

原项目为一个财商教育类网页游戏的本地工程，因存在品牌/IP 相关法律风险，需进行全面重命名、内容改写与开源部署。

## 二、目标

1. 移除所有可能引发法律风险的品牌标识、术语与股票代码。
2. 将项目重命名为原创品牌 `Ledger`。
3. 上传到 GitHub 并配置 GitHub Pages 静态部署。
4. 确保所有目录、文件名、代码引用、文案一致更新。

## 三、技术栈

- 框架：Vue 3 + TypeScript

- 构建工具：Vite 8

- 样式：Tailwind CSS 4

- 状态管理：Pinia 3

- 路由：Vue Router 5

- 图标：Lucide Vue Next

- 部署：GitHub Actions → GitHub Pages

## 四、完成内容

### 4.1 品牌重命名

- 项目名称改为 `Ledger - 财商教育模拟游戏`。

- 移除原 `101` 数字标识。

- 移除所有 `现金流`、`穷爸爸`、`富爸爸` 等原版术语。

- 将 `现金流日` 统一改为 `被动收入日`。

### 4.2 股票代码替换

原股票代码已全部替换为原创名称：

| 原代码        | 新代码      |
| ---------- | -------- |
| ON2U       | NOVA     |
| MYT4U      | MEDX     |
| OK4U       | TECH     |
| GRO4US     | GROW     |
| DTF        | DYNA     |
| ON2U-ETF   | NOVA-ETF |
| MYT4U-ETF  | MEDX-ETF |
| OK4U-ETF   | TECH-ETF |
| GRO4US-ETF | GROW-ETF |
| DTF-ETF    | DYNA-ETF |

涉及文件：`src/data/cards.ts` 及相关卡片描述。

### 4.3 目录与文件重命名

- 项目根目录：`cashflow101.web` → `/`（根目录）

- 包名：`ledger-game`

- 入口 HTML、配置文件、组件路径均已同步调整。

### 4.4 GitHub 仓库

- 仓库地址：<https://github.com/zuobotao/ledger-game.git>

- 默认分支：`main`

- 最新提交：`c7d7d66` - `rebrand: replace all remaining stock tickers and cashflow day references`

### 4.5 GitHub Pages 部署配置

- 已创建 `.github/workflows/deploy.yml` 工作流。

- `vite.config.ts` 中 `base` 已设置为 `/ledger-game/`。

- 预计访问地址：<https://zuobotao.github.io/ledger-game/>

## 五、剩余操作

GitHub Pages 需手动启用：

1. 打开仓库：<https://github.com/zuobotao/ledger-game>
2. 进入 `Settings → Pages`
3. `Source` 选择 `GitHub Actions`
4. 保存后等待工作流执行完成即可访问。

## 六、验证结果

- 已全局搜索并确认无残留风险关键词（ON2U、MYT4U、穷爸爸、富爸爸、现金流日等）。

- 项目可正常执行 `npm install`、`npm run build`、`npm run dev`。

- 构建产物已适配 GitHub Pages 子路径部署。

## 七、注意事项

- `Rat Race` 与 `Fast Track` 作为通用英文表述保留在组件内部命名中，未出现在用户可见文案。

- 所有财务规则与玩法逻辑未做功能变更，仅做文案与标识替换。

- 后续如需进一步降低风险，可考虑替换所有职业名称、城市地名等现实指向性文案。

## 八、目录结构

```
/
├── .github/workflows/deploy.yml   # GitHub Pages 部署
├── docs/                          # 项目文档
├── public/                        # 静态资源
├── src/
│   ├── assets/                    # 图片/样式
│   ├── components/                # 组件
│   ├── data/cards.ts              # 卡片数据（股票代码已替换）
│   ├── router/                    # 路由
│   ├── stores/                    # Pinia 状态
│   ├── views/                     # 页面
│   ├── App.vue
│   └── main.ts
├── test/                          # 测试
├── index.html
├── package.json
├── README.md
├── tsconfig.json
└── vite.config.ts                 # base 配置
```

***

生成时间：2026-09-03
